[project]
name = "discord-memory"
version = "0.1.0"
description = "Accurate, scalable, cost-effective agentic memory layer for Discord bots."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "discord-memory contributors" }]
keywords = ["discord", "memory", "agents", "llm", "rag"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Communications :: Chat",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Typing :: Typed",
]
dependencies = [
    "pydantic>=2.9",
    "httpx>=0.27",
]

[project.optional-dependencies]
postgres = ["asyncpg>=0.29"]
local-embeddings = ["sentence-transformers>=3.0"]
discord = ["discord.py>=2.4"]

[project.urls]
Homepage = "https://github.com/nulzo/discord-memory"
Documentation = "https://github.com/nulzo/discord-memory/tree/main/docs"

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "pytest-cov>=5.0",
    "ruff>=0.8",
    "mypy>=1.13",
    "discord-py>=2.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/discord_memory"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-q"

[tool.coverage.run]
source = ["src/discord_memory"]
branch = true

[tool.coverage.report]
fail_under = 90
show_missing = true

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
ignore = ["SIM108"]  # ternaries are not always clearer

[tool.mypy]
python_version = "3.12"
strict = true
mypy_path = "src"
packages = ["discord_memory"]
disallow_untyped_decorators = false

[[tool.mypy.overrides]]
module = ["sentence_transformers.*"]
ignore_missing_imports = true
